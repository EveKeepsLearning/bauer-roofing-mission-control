'use strict';
(async function(){
 const $=id=>document.getElementById(id),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),fmt=v=>Number(v||0).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2}),date=v=>{if(!v)return'';const d=new Date(String(v).slice(0,10)+'T12:00:00');return d.toLocaleDateString('en-US',{month:'numeric',day:'numeric',year:'numeric'});};
 function fail(message){$('error').textContent=message;$('error').hidden=false;}
 try{const id=new URLSearchParams(location.search).get('id');if(!id)throw new Error('No payment request was selected.');const cfg=window.BAUER_CONFIG||{},db=supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_ANON_KEY),auth=await db.auth.getSession();if(!auth.data.session){location.href='index.html';return;}const hr=await db.from('subcontractor_payment_requests').select('*').eq('id',id).single();if(hr.error)throw hr.error;const r=hr.data;$('backBtn').href='jobs.html?job='+encodeURIComponent(r.job_id);const ir=await db.from('subcontractor_payment_request_items').select('*').eq('payment_request_id',id).order('sort_order');if(ir.error)throw ir.error;const priorR=await db.from('subcontractor_payment_requests').select('id,retainage_percent,requisition_number').eq('job_id',r.job_id).eq('subcontractor_id',r.subcontractor_id).lt('requisition_number',r.requisition_number);if(priorR.error)throw priorR.error;let priorGross=0,previousPayments=0;if(priorR.data.length){const pi=await db.from('subcontractor_payment_request_items').select('payment_request_id,requested_amount').in('payment_request_id',priorR.data.map(x=>x.id));if(pi.error)throw pi.error;for(const pr of priorR.data){const gross=pi.data.filter(x=>x.payment_request_id===pr.id).reduce((s,x)=>s+Number(x.requested_amount||0),0);priorGross+=gross;previousPayments+=gross*(1-Number(pr.retainage_percent||0)/100);}}
 const rows=ir.data||[],contractTotal=rows.reduce((s,x)=>s+Number(x.contract_amount||0),0),currentGross=rows.reduce((s,x)=>s+Number(x.requested_amount||0),0),workToDate=priorGross+currentGross,retainage=workToDate*Number(r.retainage_percent||0)/100,earned=workToDate-retainage,amount=earned-previousPayments,locationLine=[r.subcontractor_city_snapshot,r.subcontractor_state_snapshot,r.subcontractor_zip_snapshot].filter(Boolean).join(', '),contact=r.subcontractor_contact_snapshot||r.subcontractor_name_snapshot;
 const blankItems=Math.max(0,4-rows.length);$('sheet').innerHTML=`<div class="top"><div><div class="sub-name">${esc(r.subcontractor_name_snapshot)}</div><div>${esc(r.subcontractor_street_snapshot||'')}</div><div>${esc(locationLine)}</div></div><div class="company"><h1>BAUER ROOFING</h1><div>1430 Congaree Dr</div><div>West Columbia, SC 29172</div></div></div><div class="title">REQUEST FOR PAYMENT</div><div class="meta"><div class="line"><b>Requisition No.</b><span class="fill">${esc(r.requisition_number)}</span></div><div class="line"><b>DATE:</b><span class="fill">${esc(date(r.payment_date))}</span></div><div class="line"><b>Project:</b><span class="fill">${esc([r.job_number_snapshot,r.customer_name_snapshot].filter(Boolean).join(' - '))}</span></div><div class="line"><b>Address:</b><span class="fill">${esc(r.property_address_snapshot||'')}</span></div></div><p class="intro">To: Mr. Bauer &nbsp;&nbsp; Application is made for payment, as shown below, in connection with work through <b>${esc(date(r.work_date))}</b>.</p><div class="body"><div><div class="section-title">Original Contract Amount</div>${rows.map(x=>`<div class="item-row"><span>${esc(x.description)}</span><span>${fmt(x.contract_amount)}</span></div>`).join('')}${Array.from({length:blankItems},()=>'<div class="item-row"><span>&nbsp;</span><span>&nbsp;</span></div>').join('')}<div class="money-row"><b>Total Revised Contract</b><span class="amt">${fmt(contractTotal)}</span></div><div class="section-title">Work Performed to Date</div>${rows.map(x=>`<div class="item-row"><span>${esc(x.description)}</span><span>${fmt(Number(x.requested_amount||0)+priorGross*(Number(x.requested_amount||0)/(currentGross||1)))}</span></div>`).join('')}<div class="totals"><div class="money-row"><span>Total Work Performed</span><span class="amt">${fmt(workToDate)}</span></div><div class="money-row"><span>Less ${fmt(r.retainage_percent)}% Retainage</span><span class="amt">${fmt(retainage)}</span></div><div class="money-row"><span>Earned to Date</span><span class="amt">${fmt(earned)}</span></div><div class="money-row"><span>Less Previous Payments</span><span class="amt">${fmt(previousPayments)}</span></div><div class="money-row"><span>This Requisition</span><span class="amt">${fmt(amount)}</span></div></div></div><aside class="no-write"><h3>DO NOT WRITE IN THIS SPACE</h3>${Array.from({length:15},()=>'<div class="write-line"></div>').join('')}</aside></div><p class="release">The undersigned contractor certifies that the work covered by this request has been completed in accordance with the contract documents and that all amounts previously paid have been used to pay obligations connected with the work covered by prior requests for payment.</p><div class="signature"><div><div class="sig-line">${esc(r.subcontractor_name_snapshot)}</div><div class="sig-label">Company</div></div><div><div class="sig-line">${esc(contact)}</div><div class="sig-label">Authorized signature / printed name</div></div></div>`;$('sheet').hidden=false;document.title=`RFP ${r.requisition_number} - ${r.subcontractor_name_snapshot}`;$('printBtn').onclick=()=>window.print();
 }catch(e){fail('Request for Payment could not be opened: '+(e.message||String(e)));}
})();
