'use strict';
const cfg=window.BAUER_CONFIG||{};const $=id=>document.getElementById(id);let db=null;let jobs=[];let documents=[];let activeView='timeline';let weekStart=startOfWeek(new Date());let directJobOpened=false;let tableSort={key:null,dir:'asc'};
const STAGES=[['awarded','Awarded'],['contract','Contract / Deposit'],['material','Material Ordered'],['ready','Ready to Schedule'],['scheduled','Scheduled'],['delivered','Material Delivered'],['production','In Production'],['complete','Work Complete'],['closeout','Final Payment / Closeout']];
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));}
function notice(t,type=''){const n=$('notice');n.textContent=t;n.className=`notice ${type}`.trim();}
function iso(v){return String(v||'').slice(0,10)}function dateLabel(v){if(!v)return'—';const d=new Date(String(v).slice(0,10)+'T12:00:00');return Number.isNaN(d.getTime())?String(v):d.toLocaleDateString(undefined,{month:'short',day:'numeric',year:'numeric'});}function startOfWeek(d){const x=new Date(d);x.setHours(12,0,0,0);x.setDate(x.getDate()-x.getDay());return x;}function addDays(d,n){const x=new Date(d);x.setDate(x.getDate()+n);return x;}function sameDay(a,b){return iso(a)===iso(b.toLocaleDateString('en-CA'));}
function jobType(j){return j.job_type||j.primary_category||'Job';}
function stageKey(j){const raw=String(j.stage||'').toLowerCase();if(j.completion_date)return'complete';if(j.production_started_date||raw.includes('in production'))return'production';if(raw.includes('final payment')||raw.includes('closeout'))return'closeout';if(j.material_delivery_date||raw.includes('material delivered'))return'delivered';if(j.confirmed_start_date||j.target_start_date||raw==='scheduled')return'scheduled';if(raw.includes('ready to schedule'))return'ready';if(j.material_ordered_date||raw.includes('material ordered'))return'material';if(raw.includes('deposit')||raw.includes('contract'))return'contract';return'awarded';}
function stageLabel(j){return STAGES.find(([k])=>k===stageKey(j))?.[1]||'Awarded';}
function nextStep(j){if(j.production_blocker)return j.production_blocker;if(j.client_communication_needed)return j.client_communication_reason||'Customer update needed';const s=stageKey(j);if(s==='awarded')return j.deposit_status&&String(j.deposit_status).toLowerCase().includes('received')?'Prepare materials / schedule':'Confirm contract and deposit';if(s==='contract')return'Order materials';if(s==='material')return j.confirmed_start_date?'Prepare for start':'Schedule installation';if(s==='ready')return'Set install date';if(s==='scheduled')return j.material_delivery_date?'Confirm delivery / crew':'Confirm materials and crew';if(s==='delivered')return'Start production';if(s==='production')return'Complete work / final inspection';if(s==='complete')return'Final invoice / closeout';return'Close job';}
function attentionReasons(j){const reasons=[];const today=new Date();today.setHours(12,0,0,0);if(j.production_blocker)reasons.push(j.production_blocker);if(j.client_communication_needed)reasons.push(j.client_communication_reason||'Customer contact needed');if(j.client_communication_due_date&&new Date(j.client_communication_due_date+'T12:00:00')<=today)reasons.push('Customer update due');if(stageKey(j)==='awarded'&&!j.confirmed_start_date&&!j.target_start_date){const d=new Date((j.contract_date||j.sale_date||j.created_at||'').slice(0,10)+'T12:00:00');if(!Number.isNaN(d)&&((today-d)/86400000)>=7)reasons.push('No start date');}if(j.production_last_update_at){const d=new Date(j.production_last_update_at);if((Date.now()-d.getTime())/86400000>7)reasons.push('No recent production update');}return [...new Set(reasons)];}
function activeJobs(){return jobs.filter(j=>!j.deleted_at&&!j.archived_at&&!j.contract_canceled_at&&!['cancelled','canceled','contract canceled','final / closed','fully paid','closed'].includes(String(j.stage||'').toLowerCase()));}
function filtered(){const q=$('jobSearch').value.trim().toLowerCase(),t=$('jobTypeFilter').value;return activeJobs().filter(j=>(!t||jobType(j)===t)&&(!q||[j.job_number,j.customer_name,j.property_address,j.lead_number,j.stage,j.installer,jobType(j),j.production_notes].join(' ').toLowerCase().includes(q)));}
const TABLE_KEYS=['job','customer','type','stage','start','installer','next'];
function tableValue(j,key){if(key==='job')return j.job_number||'';if(key==='customer')return j.customer_name||'';if(key==='type')return jobType(j);if(key==='stage')return stageLabel(j);if(key==='start')return j.confirmed_start_date||j.target_start_date||'';if(key==='installer')return j.installer||'';if(key==='next')return nextStep(j);return'';}
function sortTableRows(rows){if(!tableSort.key)return rows;const dir=tableSort.dir==='desc'?-1:1;return [...rows].sort((a,b)=>{const av=tableValue(a,tableSort.key),bv=tableValue(b,tableSort.key);if(!av&&!bv)return 0;if(!av)return 1;if(!bv)return-1;if(tableSort.key==='start'){const at=new Date(String(av).slice(0,10)+'T12:00:00').getTime(),bt=new Date(String(bv).slice(0,10)+'T12:00:00').getTime();return(at-bt)*dir;}return String(av).localeCompare(String(bv),undefined,{numeric:true,sensitivity:'base'})*dir;});}
function refreshSortHeaders(){document.querySelectorAll('.jobs-table thead th').forEach((th,i)=>{if(!th.dataset.sortLabel)th.dataset.sortLabel=th.textContent.trim();const key=TABLE_KEYS[i];th.style.cursor='pointer';th.title='Click to sort ascending or descending';th.setAttribute('aria-sort',tableSort.key===key?(tableSort.dir==='asc'?'ascending':'descending'):'none');th.textContent=th.dataset.sortLabel+(tableSort.key===key?(tableSort.dir==='asc'?' ▲':' ▼'):' ↕');});}
function setTableSort(key){if(tableSort.key===key)tableSort.dir=tableSort.dir==='asc'?'desc':'asc';else tableSort={key,dir:'asc'};renderTable();}
function card(j){const att=attentionReasons(j);return `<div class="job-card" data-job-id="${esc(j.id)}"><b>${esc(j.job_number||'Job')}</b><div>${esc(j.customer_name||'Unnamed customer')}</div><div class="job-meta">${esc(jobType(j))}</div><div class="job-meta">${j.confirmed_start_date?'Start: '+esc(dateLabel(j.confirmed_start_date)):j.contract_date?'Contract: '+esc(dateLabel(j.contract_date)):''}</div><span class="job-badge ${att.length?'attention-badge':''}">${esc(att[0]||nextStep(j))}</span></div>`;}
function renderTimeline(){const rows=filtered();$('timelineBoard').innerHTML=STAGES.map(([key,label])=>{const group=rows.filter(j=>stageKey(j)===key);return `<div class="stage-col stage-${key}"><div class="stage-head">${label}<small>${group.length} job${group.length===1?'':'s'}</small></div>${group.map(card).join('')||'<div class="job-meta">No jobs</div>'}</div>`}).join('');}
function renderTable(){const rows=sortTableRows(filtered());$('jobsTableBody').innerHTML=rows.map(j=>`<tr data-job-id="${esc(j.id)}"><td><b>${esc(j.job_number||'')}</b></td><td>${esc(j.customer_name||'')}</td><td>${esc(jobType(j))}</td><td>${esc(stageLabel(j))}</td><td>${esc(dateLabel(j.confirmed_start_date||j.target_start_date))}</td><td>${esc(j.installer||'')}</td><td>${esc(nextStep(j))}</td></tr>`).join('')||'<tr><td colspan="7" class="empty">No active jobs match.</td></tr>';refreshSortHeaders();}
function renderAttention(){const rows=filtered().map(j=>({j,reasons:attentionReasons(j)})).filter(x=>x.reasons.length);$('attentionList').innerHTML=rows.length?`<div class="attention-row" style="font-weight:700;background:#eef3f8"><div>Job #</div><div>Customer</div><div>Issue</div><div>Start</div><div>Next step</div></div>`+rows.map(({j,reasons})=>`<div class="attention-row" data-job-id="${esc(j.id)}"><div><b>${esc(j.job_number||'')}</b></div><div>${esc(j.customer_name||'')}</div><div>${esc(reasons.join(' • '))}</div><div>${esc(dateLabel(j.confirmed_start_date||j.target_start_date))}</div><div>${esc(nextStep(j))}</div></div>`).join(''):'<div class="empty">Nothing currently needs attention.</div>';}
function renderCalendar(){const days=Array.from({length:7},(_,i)=>addDays(weekStart,i));$('weekLabel').textContent=`${days[0].toLocaleDateString(undefined,{month:'short',day:'numeric'})} – ${days[6].toLocaleDateString(undefined,{month:'short',day:'numeric',year:'numeric'})}`;const rows=[['Material Delivery','material_delivery_date'],['Production Start','confirmed_start_date'],['Expected Completion','expected_completion_date'],['Confirmed Completion','completion_date']];let html='<div class="cal-cell cal-head"></div>'+days.map(d=>`<div class="cal-cell cal-head">${d.toLocaleDateString(undefined,{weekday:'short',month:'short',day:'numeric'})}</div>`).join('');for(const [label,field] of rows){html+=`<div class="cal-cell cal-head">${label}</div>`;for(const d of days){const events=filtered().filter(j=>sameDay(j[field],d));html+=`<div class="cal-cell">${events.map(j=>`<div class="cal-event" data-job-id="${esc(j.id)}"><b>${esc(j.job_number||'')}</b><br>${esc(j.customer_name||'')}</div>`).join('')}</div>`;}}$('calendarGrid').innerHTML=html;}
function renderAll(){renderTimeline();renderCalendar();renderAttention();renderTable();}
function setView(v){activeView=v;document.querySelectorAll('[data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===v));['timeline','calendar','attention','table'].forEach(x=>$(x+'View').classList.toggle('hidden',x!==v));}
function docsForJob(jobId){return documents.filter(d=>d.job_id===jobId).sort((a,b)=>String(b.document_date||b.created_at||'').localeCompare(String(a.document_date||a.created_at||'')));}
function renderDocuments(jobId){const rows=docsForJob(jobId);$('jobDocumentList').innerHTML=rows.length?rows.map(d=>`<div class="doc-row"><div><div class="doc-type">${esc(d.document_type)}</div><div class="doc-notes">${esc(d.added_by?`Added by ${d.added_by}`:'')}</div></div><div><b>${esc(d.file_name)}</b><div class="doc-notes">${esc(d.notes||'')}</div></div><div>${esc(dateLabel(d.document_date||d.created_at))}</div><div class="doc-notes">${esc(d.onedrive_url)}</div><div class="doc-actions"><a class="btn small" href="${esc(d.onedrive_url)}" target="_blank" rel="noopener noreferrer">Open in OneDrive</a><button class="btn small" type="button" data-edit-document="${esc(d.id)}">Edit</button><button class="btn small" type="button" data-delete-document="${esc(d.id)}">Delete</button></div></div>`).join(''):'<div class="empty">No linked documents yet.</div>';}
function openJob(id){const j=jobs.find(x=>x.id===id);if(!j)return;$('editJobId').value=j.id;$('jobDialogTitle').textContent=`Job ${j.job_number||''} – ${j.customer_name||''}`;$('editCustomer').value=j.customer_name||'';$('editJobNumber').value=j.job_number||'';$('editInquiryNumber').value=j.lead_number||'';$('editJobType').value=jobType(j);$('editStage').value=j.contract_canceled_at||String(j.stage).toLowerCase()==='contract canceled'?'Contract Canceled':stageLabel(j);$('editContractDate').value=iso(j.contract_date);$('editMaterialOrdered').value=iso(j.material_ordered_date);$('editMaterialDelivery').value=iso(j.material_delivery_date);$('editStart').value=iso(j.confirmed_start_date);$('editExpected').value=iso(j.expected_completion_date);$('editCompleted').value=iso(j.completion_date);$('editInstaller').value=j.installer||'';$('editCommDue').value=iso(j.client_communication_due_date);$('editAddress').value=j.property_address||'';$('editBlocker').value=j.production_blocker||'';$('editNotes').value=j.production_notes||'';const canceled=Boolean(j.contract_canceled_at)||String(j.stage||'').toLowerCase()==='contract canceled';$('cancelContractBtn').style.display=canceled?'none':'inline-flex';if(canceled){$('canceledInfo').className='notice';$('canceledInfo').textContent=`Contract canceled ${j.contract_canceled_at?new Date(j.contract_canceled_at).toLocaleDateString():''}${j.contract_cancellation_reason?` — ${j.contract_cancellation_reason}`:''}`;}else{$('canceledInfo').className='notice hidden';$('canceledInfo').textContent='';}renderDocuments(j.id);$('jobDialog').showModal();}
function openDocumentDialog(doc=null){const jobId=$('editJobId').value;if(!jobId)return;const d=doc||{};$('documentDialogTitle').textContent=doc?'Edit Document Link':'Add Document Link';$('documentId').value=d.id||'';$('documentJobId').value=jobId;$('documentType').value=d.document_type||'Contract';$('documentDate').value=iso(d.document_date);$('documentFileName').value=d.file_name||'';$('documentUrl').value=d.onedrive_url||'';$('documentNotes').value=d.notes||'';$('documentAddedBy').value=d.added_by||'';$('documentDialog').showModal();}
async function saveDocument(){const id=$('documentId').value,jobId=$('documentJobId').value,fileName=$('documentFileName').value.trim(),url=$('documentUrl').value.trim();if(!fileName)return notice('File name is required.','error');if(!/^https?:\/\//i.test(url))return notice('Paste a valid OneDrive link beginning with http:// or https://.','error');const row={job_id:jobId,document_type:$('documentType').value,file_name:fileName,onedrive_url:url,document_date:$('documentDate').value||null,notes:$('documentNotes').value.trim()||null,added_by:$('documentAddedBy').value.trim()||null,updated_at:new Date().toISOString()};let result;if(id)result=await db.from('job_document_links').update(row).eq('id',id).select('*').single();else result=await db.from('job_document_links').insert(row).select('*').single();if(result.error)return notice(result.error.message,'error');if(id){documents=documents.map(d=>d.id===id?result.data:d);}else documents.push(result.data);$('documentDialog').close();renderDocuments(jobId);notice(id?'Document link updated.':'Document link added.','success');}
async function deleteDocument(id){const d=documents.find(x=>x.id===id);if(!d)return;if(!confirm(`Delete the BRO link for ${d.file_name}? This does not delete the file from OneDrive.`))return;const {error}=await db.from('job_document_links').delete().eq('id',id);if(error)return notice(error.message,'error');documents=documents.filter(x=>x.id!==id);renderDocuments(d.job_id);notice('Document link removed. The OneDrive file was not deleted.','success');}
async function cancelContract(){const id=$('editJobId').value;const j=jobs.find(x=>x.id===id);if(!j)return;const reason=prompt('Reason the contract was canceled (optional):','');if(reason===null)return;const by=prompt('Canceled by (optional):','Eve');if(by===null)return;if(!confirm(`Cancel the contract for ${j.customer_name||j.job_number||'this job'}? The record will be preserved and removed from Open Jobs.`))return;const patch={stage:'Contract Canceled',contract_canceled_at:new Date().toISOString(),contract_cancellation_reason:String(reason||'').trim()||null,contract_canceled_by:String(by||'').trim()||null,archived_at:new Date().toISOString(),closed_at:new Date().toISOString(),updated_at:new Date().toISOString()};const {error}=await db.from('jobs').update(patch).eq('id',id);if(error)return notice(error.message,'error');$('jobDialog').close();await load();notice('Contract canceled. The job record was preserved and removed from Open Jobs.','success');}
async function saveJob(){const id=$('editJobId').value;const existing=jobs.find(j=>j.id===id);const patch={customer_name:$('editCustomer').value.trim(),job_number:$('editJobNumber').value.trim()||null,lead_number:$('editInquiryNumber').value.trim()||null,job_type:$('editJobType').value.trim()||null,primary_category:$('editJobType').value.trim()||null,stage:$('editStage').value,contract_date:$('editContractDate').value||null,material_ordered_date:$('editMaterialOrdered').value||null,material_delivery_date:$('editMaterialDelivery').value||null,confirmed_start_date:$('editStart').value||null,expected_completion_date:$('editExpected').value||null,completion_date:$('editCompleted').value||null,installer:$('editInstaller').value.trim()||null,client_communication_due_date:$('editCommDue').value||null,property_address:$('editAddress').value.trim()||null,production_blocker:$('editBlocker').value.trim()||null,production_notes:$('editNotes').value.trim()||null,updated_at:new Date().toISOString()};if(existing?.contract_canceled_at){patch.stage='Contract Canceled';patch.archived_at=existing.archived_at||new Date().toISOString();}else if(patch.completion_date){patch.stage='Work Complete';}else if(patch.stage==='Work Complete'){patch.stage='In Production';}const {error}=await db.from('jobs').update(patch).eq('id',id);if(error)return notice(error.message,'error');$('jobDialog').close();await load();notice('Job updated.','success');}
async function load(){const [jobRes,docRes]=await Promise.all([db.from('jobs').select('*').order('updated_at',{ascending:false}).limit(2000),db.from('job_document_links').select('*').order('created_at',{ascending:false}).limit(5000)]);if(jobRes.error)return notice(jobRes.error.message,'error');if(docRes.error)return notice(docRes.error.message,'error');jobs=jobRes.data||[];documents=docRes.data||[];const types=[...new Set(jobs.map(jobType).filter(Boolean))].sort();$('jobTypeFilter').innerHTML='<option value="">All job types</option>'+types.map(t=>`<option>${esc(t)}</option>`).join('');renderAll();const direct=new URLSearchParams(location.search).get('job');if(direct&&!directJobOpened){directJobOpened=true;openJob(direct);}}
async function start(){db=supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_ANON_KEY);const {data}=await db.auth.getSession();if(!data.session){location.href='index.html';return;}await load();}
document.querySelectorAll('.view-tab').forEach(b=>b.onclick=()=>setView(b.dataset.view));document.querySelectorAll('.jobs-table thead th').forEach((th,i)=>th.addEventListener('click',()=>setTableSort(TABLE_KEYS[i])));$('jobSearch').oninput=renderAll;$('jobTypeFilter').onchange=renderAll;$('prevWeek').onclick=()=>{weekStart=addDays(weekStart,-7);renderCalendar()};$('nextWeek').onclick=()=>{weekStart=addDays(weekStart,7);renderCalendar()};$('addDocumentBtn').onclick=()=>openDocumentDialog();$('saveDocumentBtn').onclick=saveDocument;$('cancelContractBtn').onclick=cancelContract;document.body.addEventListener('click',e=>{const editDoc=e.target.closest('[data-edit-document]');if(editDoc){e.preventDefault();e.stopPropagation();return openDocumentDialog(documents.find(d=>d.id===editDoc.dataset.editDocument));}const delDoc=e.target.closest('[data-delete-document]');if(delDoc){e.preventDefault();e.stopPropagation();return deleteDocument(delDoc.dataset.deleteDocument);}const el=e.target.closest('[data-job-id]');if(el)openJob(el.dataset.jobId)});$('saveJobBtn').onclick=saveJob;start();